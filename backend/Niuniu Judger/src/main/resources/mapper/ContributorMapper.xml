<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.Niuniu_Judger.mapper.ContributorMapper">

    <!-- Insert contributor -->
    <insert id="insertContributor" parameterType="com.example.Niuniu_Judger.model.Contributor">
        INSERT INTO contributors (username, project_id, contributions)
        VALUES (#{username}, #{projectId}, #{contributions})
    </insert>

    <!-- Update contributor contributions -->
    <update id="updateContributorContributions">
        UPDATE contributors
        SET contributions = #{contributions}
        WHERE username = #{username} AND project_id = #{projectId}
    </update>

    <!-- Select contributors by project ID -->
    <select id="selectContributorsByProjectId" parameterType="String" resultType="com.example.Niuniu_Judger.model.Contributor">
        SELECT id, username, project_id AS projectId, contributions
        FROM contributors
        WHERE project_id = #{projectId}
    </select>

    <!-- Select contributor by username and project ID -->
    <select id="selectContributorByUsernameAndProjectId" parameterType="map" resultType="com.example.Niuniu_Judger.model.Contributor">
        SELECT id, username, project_id AS projectId, contributions
        FROM contributors
        WHERE username = #{username} AND project_id = #{projectId}
    </select>

</mapper>
